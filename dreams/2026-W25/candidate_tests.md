# Test candidates — 2026-W25

> 2 candidate(s). Threshold: one candidate per failure event.

## failure-agent-delta-missing-r-drm-999-implementation

- Evidence count: 1
- Notes: failure event from actor `agent-delta`

Failure observed for actor `agent-delta` with payload: `error` = `missing R-DRM-999 implementation`.

Promote into the eval suite as a regression test if the failure shape is reproducible.

## failure-agent-gamma-timeout-on-audit-log-write

- Evidence count: 1
- Notes: failure event from actor `agent-gamma`

Failure observed for actor `agent-gamma` with payload: `error` = `timeout on audit-log-write`.

Promote into the eval suite as a regression test if the failure shape is reproducible.
